package com.kaiser.aiagent.tools.future

/**
 * PLACEHOLDER — Accessibility tools (v0.5+)
 *
 * Will host tools that read and manipulate the on-screen UI of other
 * apps via an AccessibilityService:
 *   - read_screen_text
 *   - find_element_by_text
 *   - tap_element
 *   - type_text
 *
 * SECURITY: these tools require the user to manually enable the
 * AccessibilityService in system settings. They will not be registered
 * automatically. v0.3 does NOT implement any of this.
 */
internal object AccessibilityToolsPlaceholder
