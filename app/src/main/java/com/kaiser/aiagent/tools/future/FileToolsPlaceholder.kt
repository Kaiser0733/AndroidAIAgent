package com.kaiser.aiagent.tools.future

/**
 * PLACEHOLDER — File tools (v0.4+)
 *
 * Will host file-system tools the agent can invoke:
 *   - list_directory
 *   - read_file
 *   - write_file
 *   - delete_file
 *
 * SECURITY: every file tool must respect scoped storage (app-specific
 * dirs by default; SAF for user-picked dirs; no global FS access
 * without explicit user consent). None of this is implemented at v0.3.
 */
internal object FileToolsPlaceholder
