package com.kaiser.aiagent.future.automation

/**
 * PLACEHOLDER — Automation module (v0.2+)
 *
 * Will host UI-automation pipelines: sequences of accessibility actions
 * (tap / swipe / type / scroll) that the agent composes to carry out a
 * user goal. Depends on the accessibility module.
 *
 * Nothing here is wired up at v0.1.
 */
internal object AutomationModulePlaceholder
