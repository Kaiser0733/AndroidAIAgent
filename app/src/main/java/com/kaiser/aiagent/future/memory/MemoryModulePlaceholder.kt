package com.kaiser.aiagent.future.memory

/**
 * PLACEHOLDER — Memory module (v0.2+)
 *
 * Will host long-term + working memory stores for the agent.
 * Expected backends: Room (structured facts), DataStore (preferences),
 * vector store (semantic recall).
 *
 * Nothing here is wired up at v0.1.
 */
internal object MemoryModulePlaceholder
