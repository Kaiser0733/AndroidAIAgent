package com.kaiser.aiagent.future.tools

/**
 * PLACEHOLDER — Tools module (v0.2+)
 *
 * Will host the pluggable tool/function-call registry that the chat agent
 * can invoke (e.g. open URL, send message, read contact, run script).
 *
 * Nothing here is wired up at v0.1.
 */
internal object ToolsModulePlaceholder
