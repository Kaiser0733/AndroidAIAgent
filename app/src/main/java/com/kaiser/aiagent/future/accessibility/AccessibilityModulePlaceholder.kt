package com.kaiser.aiagent.future.accessibility

/**
 * PLACEHOLDER — Accessibility module (v0.2+)
 *
 * Will host the AccessibilityService implementation that gives the agent
 * the ability to read and manipulate the on-screen UI of other apps.
 *
 * The actual service class (annotated with `@AndroidAccessibilityService`)
 * will live in the manifest as a `<service>` element and will be enabled
 * from Settings once implemented. At v0.1 nothing here is declared in the
 * manifest and the user is NOT asked to enable any accessibility service.
 *
 * Nothing here is wired up at v0.1.
 */
internal object AccessibilityModulePlaceholder
