package com.kaiser.aiagent.future.chat

/**
 * PLACEHOLDER — Chat module (v0.2+)
 *
 * Will host the conversational interface backed by GLM / other LLMs.
 * Expected responsibilities in a future release:
 *   - Conversation orchestration (multi-turn, streaming)
 *   - System prompt assembly
 *   - Tool-call dispatch into the [com.kaiser.aiagent.future.tools] module
 *   - Persistence into the [com.kaiser.aiagent.future.memory] module
 *
 * Nothing here is wired up at v0.1. This file exists so future work has a
 * clear home and the package layout is stable from day one.
 */
internal object ChatModulePlaceholder
