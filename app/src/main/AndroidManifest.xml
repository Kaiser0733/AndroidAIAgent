<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- v0.6: For open_app tool — query and launch other apps -->
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES"
        tools:ignore="QueryAllPackagesPermission" />
    <!-- v0.6.2: SYSTEM_ALERT_WINDOW — lets the floating chat overlay
         draw on top of other apps so the user can keep chatting with
         the agent while the agent drives another app (YouTube, etc.). -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <!-- v0.6.2: FOREGROUND_SERVICE — keeps FloatingChatService alive
         while the app is backgrounded. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE"
        tools:ignore="ForegroundServiceType" />
    <!-- Storage access for file tools (v0.4) -->
    <!-- Android 12 and below -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="29" />
    <!-- Android 13+ granular media permissions -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />
    <!-- v0.4.1: MANAGE_EXTERNAL_STORAGE lets the agent see ALL files in
         shared storage (Downloads, Documents, etc.), not just files the
         app itself created. Required for the file-discovery tools to
         actually find the user's existing PDFs, DOCXs, etc. on Android 10+.
         The user must grant this manually via system settings — Android
         does not allow it to be granted via the normal permission dialog.
         The app opens the system settings page via SettingsActivity. -->
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE"
        tools:ignore="ScopedStorage" />

    <application
        android:name=".AndroidAIAgentApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AndroidAIAgent"
        tools:targetApi="34">

        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:configChanges="screenSize|smallestScreenSize|screenLayout|orientation"
            android:theme="@style/Theme.AndroidAIAgent">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
          v0.6.2: Accessibility service that lets the agent read the
          screen, tap buttons, type into text fields, scroll, and press
          Back/Home. The user must enable this manually in system
          Settings → Accessibility → AI Agent. Without it the
          accessibility tools (read_screen, tap_text, type_text, scroll,
          go_back, go_home) all return ERROR.
        -->
        <service
            android:name=".accessibility.AgentAccessibilityService"
            android:exported="false"
            android:label="@string/app_name"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>

        <!--
          v0.6.2: Floating chat overlay service. Started by OpenAppTool
          just before it launches another app, so the AI Agent chat
          stays visible on top of the target app.
        -->
        <service
            android:name=".floating.FloatingChatService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Floating AI Agent chat overlay visible to the user while the agent drives another app." />
        </service>

        <!--
            Placeholder for the future FileProvider used to share downloaded APK
            files with the Android package installer. Not yet wired up at v0.1
            but declared here so the manifest is ready for the updater module.
        -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_provider_paths" />
        </provider>

    </application>

</manifest>
